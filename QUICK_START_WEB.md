# 🚀 多智能体 Web 界面快速使用指南

## ⚡ 5 分钟快速启动

### 步骤 1：启动后端（新增多智能体 API）

```bash
cd web_backend
python main.py
```

后端启动后会看到：

```
🌐 启动CampusFleet AI Web后端...
==================================================
 API文档: http://localhost:8001/api/docs
 WebSocket: ws://localhost:8001/ws/simulation
 前端地址: http://localhost:3000 (开发中)
==================================================
```

### 步骤 2：启动前端

**首次运行需要安装依赖**：

```bash
cd web_frontend
npm install
```

**启动开发服务器**：

```bash
npm run dev
```

前端启动后访问：**http://localhost:3000**

### 步骤 3：创建仿真

有两种方式：

#### 方式 A：通过 API 创建（推荐）

```bash
curl -X POST http://localhost:8001/api/simulation/create \
  -H "Content-Type: application/json" \
  -d '{
    "grid_size": 10,
    "num_cars": 4,
    "strategy": "GREEDY_NEAREST",
    "enable_logging": true
  }'
```

#### 方式 B：通过 Web 界面创建

1. 访问 http://localhost:3000
2. 点击"仿真控制台"
3. 配置参数并创建仿真

### 步骤 4：添加订单

```bash
# 添加随机订单
curl -X POST http://localhost:8001/api/orders/random

# 或添加多个
for i in {1..5}; do curl -X POST http://localhost:8001/api/orders/random; done
```

### 步骤 5：启动仿真

```bash
curl -X POST http://localhost:8001/api/simulation/start
```

### 步骤 6：查看多智能体监控

在浏览器中：

1. 点击顶部导航栏的 **"🚗 多智能体监控"**
2. 实时查看所有智能体状态
3. 观察通信日志
4. 监控协作决策
5. 追踪性能指标

---

## 📱 界面导航

启动后，顶部导航栏有 4 个菜单：

| 菜单                | 路径           | 功能                 |
| ------------------- | -------------- | -------------------- |
| 📺 **仿真控制台**   | `/`            | 创建和控制仿真       |
| 🚗 **多智能体监控** | `/multi-agent` | 监控智能体系统 ✨ 新 |
| 📊 **数据分析**     | `/analytics`   | 查看性能分析         |
| ℹ️ **关于系统**     | `/about`       | 系统信息             |

---

## 🎯 多智能体监控面板功能

### 顶部统计卡片（4 个）

- 🚗 车辆智能体：总数 + 活跃数
- 📦 订单智能体：总数 + 待处理数
- 📡 通信消息：总数 + 本轮消息
- 📊 完成率：百分比 + 性能等级

### 智能体状态面板（左侧）

**3 个标签页**：

1. **车辆智能体**

   - 位置、状态（idle/busy）
   - 感知范围（5 格）
   - 决策方法（A\* pathfinding）
   - 行动状态（moving/idle）
   - 已完成订单数

2. **调度智能体**

   - 策略名称
   - 总分配次数
   - 活跃车辆数
   - 待分配订单数

3. **环境智能体**
   - 网格大小
   - 当前步骤
   - 总订单数
   - 已完成数

### 通信日志窗口（中间）

- **实时消息流**：最近 20 条
- **消息类型**：
  - 🔵 状态报告（车辆 → 调度器）
  - 🟢 任务分配（调度器 → 车辆）
  - 🟡 完成通知（车辆 → 订单系统）
- **通信统计**：各类型消息计数

### 协作决策可视化（右侧）

- **车辆利用率**：进度条显示
- **当前任务分配**：
  - 车辆 → 订单映射
  - 距离取货点
  - 预计到达时间（ETA）
  - 决策原因
- **并行执行**：同时工作的车辆数

### 实时性能图表（底部）

- **4 个关键指标**：
  - 完成率（%）
  - 已完成订单数
  - 待处理订单数
  - 车辆利用率（%）
- **性能等级**：A/B/C 评定

---

## 🔄 自动刷新

- **刷新间隔**：每 2 秒自动更新
- **手动刷新**：点击每个面板右上角的"刷新"按钮

---

## 🎓 课程作业演示流程

### 演示准备（5 分钟前）

1. 启动后端：`cd web_backend && python main.py`
2. 启动前端：`cd web_frontend && npm run dev`
3. 创建仿真：4 车、10×10 网格
4. 添加 5-8 个订单
5. 启动仿真
6. 打开多智能体监控页面
7. 确认数据正常刷新

### 演示脚本（5-8 分钟）

#### **1 分钟 - 系统介绍**

"这是一个多智能体校园配送系统的 Web 监控界面。我们可以实时查看所有智能体的状态。"

[展示顶部统计卡片]

"目前系统有 4 辆车辆智能体，X 个订单，完成率 Y%。"

#### **2 分钟 - 智能体角色**

[点击"智能体状态面板"，切换标签]

"系统包含 4 类智能体：

1. **车辆智能体**（左侧，车辆标签）

   - 每辆车都有感知、决策、行动能力
   - 感知范围 5 格，使用 A\*算法规划路径

2. **调度智能体**（左侧，调度标签）

   - 负责任务分配和协调
   - 策略：贪心最近策略

3. **订单智能体**（数据中）

   - 管理所有配送订单

4. **环境智能体**（左侧，环境标签）
   - 管理 10×10 的校园地图"

#### **2 分钟 - 通信与协作**

[指向中间和右侧面板]

"智能体间通过通信协作：

**通信日志**（中间）：

- 车辆不断向调度器报告状态
- 调度器分配任务给车辆
- 实时消息流展示通信过程

**协作决策**（右侧）：

- 车辆利用率 X%
- Y 辆车并行工作
- 每个任务分配都有决策原因"

#### **1-2 分钟 - 系统性能**

[指向底部性能面板]

"系统实时监控性能：

- 完成率：X%
- 车辆利用率：Y%
- 性能等级：Z

所有数据每 2 秒自动刷新。"

#### **1 分钟 - 总结**

"这个系统完整展示了多智能体的：

- ✅ 感知能力（车辆感知订单）
- ✅ 决策能力（A\*路径规划、任务分配）
- ✅ 行动能力（移动、取货、送货）
- ✅ 协作机制（任务分配、负载均衡）
- ✅ 通信机制（状态报告、任务分配）

谢谢大家！"

---

## ❓ 常见问题

### Q1: 前端无法连接后端？

**A**:

1. 确认后端已启动（http://localhost:8001）
2. 检查端口是否被占用
3. 查看浏览器控制台错误

### Q2: Dashboard 显示"仿真未创建"？

**A**:
需要先创建仿真：

```bash
curl -X POST http://localhost:8001/api/simulation/create \
  -H "Content-Type: application/json" \
  -d '{"grid_size": 10, "num_cars": 4, "strategy": "GREEDY_NEAREST"}'
```

### Q3: 数据不更新？

**A**:

1. 确认仿真已启动
2. 添加几个订单
3. 点击"刷新"按钮
4. 检查后端 API 是否正常响应

### Q4: 如何停止仿真？

**A**:

```bash
curl -X POST http://localhost:8001/api/simulation/stop
```

### Q5: 如何重置仿真？

**A**:

```bash
curl -X POST http://localhost:8001/api/simulation/reset
```

---

## 🛠️ 开发模式

### 修改代码后自动刷新

前端使用 Vite，修改代码后自动热更新，无需重启。

### 查看 API 文档

访问：http://localhost:8001/api/docs

可以直接在浏览器中测试所有 API。

### 调试技巧

**查看控制台日志**：

- 浏览器按 F12 打开开发者工具
- Console 标签查看前端日志
- Network 标签查看 API 请求

**后端日志**：

- 后端终端显示所有请求日志
- 查看 API 调用情况

---

## 📚 相关文档

| 文档                  | 位置                                | 说明         |
| --------------------- | ----------------------------------- | ------------ |
| **多智能体 Web 指南** | `web_frontend/README_MULTIAGENT.md` | 详细功能说明 |
| **技术方案**          | `assignment_submission/技术方案.md` | 完整技术文档 |
| **演示脚本**          | `assignment_submission/演示脚本.md` | 课堂演讲稿   |
| **API 文档**          | http://localhost:8001/api/docs      | 在线交互文档 |

---

## 🎉 完成！

现在你已经：

- ✅ 启动了完整的 Web 系统
- ✅ 集成了多智能体监控面板
- ✅ 可以实时查看所有智能体状态
- ✅ 准备好课程作业演示

**下一步**：

1. 熟悉界面操作
2. 练习演示流程
3. 准备演讲内容
4. 录制演示视频（可选）

---

**祝演示成功！** 🚀🎓✨
